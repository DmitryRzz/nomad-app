## 2026-04-24 — NOMAD App Development

### Morning Session
- User asked to continue work (Block 2: Backend API + Auth)
- Created Node.js backend with SQLite, JWT auth, demo routes (5 routes with stops)
- Backend runs on port 3000, endpoints: /auth/*, /routes, /routes/demo, /poi
- Updated Flutter client: api_service.dart, auth_provider.dart, User/Route models
- Created web compatibility stubs for mobile-only plugins (Firebase, SQLite, geolocator, camera, etc.)

### Key Issues
- Flutter web compiles successfully but shows blank screen at runtime
  - Root cause: mobile plugins still try to initialize in web build
  - Solution attempted: web stubs in lib/services/web_compat/ + main_web.dart
  - Status: NOT RESOLVED — will revisit when building actual mobile app

### HTML Prototype (Stable Fallback)
- Created updated prototype.html with Sunset design and real demo data
- 5 demo routes: Paris, Tokyo, Bali, New York, Barcelona
- Each route has 3-4 stops with descriptions
- Features: route list, route detail with stops, compass screen, profile
- Localtunnel URL (volatile): https://nasty-kiwis-smoke.loca.lt
- HTTP server on port 8888 (persistent)

### Flutter UI Updates
- Created LoginScreen and RegisterScreen with Sunset gradient design
- Updated WelcomeScreen to use SunsetGradients.primary (fixed compilation error)
- Updated main.dart with named routes: /login, /register, /routes
- Web build currently compiling (warm-shell session)

### User Context
- User tests from iPhone, needs simple access without complex setup
- Prefers web links over installing anything
- Russian-speaking, terse communication style
- GitHub repo: DmitryRzz/nomad-app (public)

### Next Steps
- Complete Flutter web build test
- If web still blank, focus on HTML prototype polish and feature completion
- Block 3: AI Trip Generation (TriPandoo features) — pending
